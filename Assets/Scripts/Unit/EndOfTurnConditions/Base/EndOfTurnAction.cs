using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public abstract class UnitEndTurnCondition
{
    Unit m_targetUnit = null;

    protected Unit targetUnit { get { return m_targetUnit; } }

    public void Init(Unit targetUnit)
    {
        m_targetUnit = targetUnit;
        Setup();
    }

    public abstract void Setup();

    public abstract void PerformAction();

    public static UnitEndTurnCondition CreateCondition(EOTConditionKey conditionKey)
    {
        return EndOfTurnConditionCreator.CreateCondition(conditionKey);
    }
}
*/